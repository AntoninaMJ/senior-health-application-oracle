spring:
  datasource:
    url: jdbc:h2:~/senior-health;MODE=Oracle
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update # auto create/update tables in db
  mail:
    host: smtp.gmail.com
    port: 587
    username: seniorhealth123@gmail.com
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

server:
  port: 8080